/**
 * 第 11 题：无限循环
 *
 * 【题目描述】
 * 小美有一个长度为 n 的数组 {a1, a2, ..., an}，将与初始数组完全相同的数组
 * 连续拼接到其末尾，共拼接 10^9 次。设拼接完成后的新数组记为 a'，则新数组
 * 的长度为 n × (10^9 + 1)，并且对于任意的 n < i <= n × (10^9 + 1)，
 * 都有 a'[i] = a'[i - n]。
 *
 * 请你计算新数组 a' 的最长严格递增子序列的长度，并输出这个长度。
 *
 * 【名词解释】
 * 子序列：从原序列中删除任意个（可以为零、可以为全部）元素后按原相对顺序
 *         得到的新序列。
 * 严格递增子序列：子序列中相邻元素的值严格递增，即若子序列为 {b1, b2, ..., bk}，
 *                 则对所有 1 <= i < k，都有 b[i] < b[i+1]。
 *
 * 【输入描述】
 * 每个测试文件均包含多组测试数据。
 * 第一行输入一个整数 T (1 <= T <= 10^4) 代表数据组数，每组测试数据描述如下：
 *   - 第一行输入一个整数 n (1 <= n <= 2×10^5)，表示原数组的长度；
 *   - 第二行输入 n 个整数 a1, a2, ..., an (1 <= ai <= n)，表示原数组的元素。
 * 除此之外，保证单个测试文件的 n 之和不超过 2×10^5。
 *
 * 【输出描述】
 * 对于每一组测试数据，新起一行，输出一个整数，表示新数组的最长严格递增子序列长度。
 *
 * 【示例】
 * 输入：
 * 2
 * 4
 * 1 1 2 3
 * 5
 * 4 5 3 3 4
 *
 * 输出：
 * 3
 * 3
 *
 * 【说明】
 * 对于第 1 组，最终最长严格递增子序列为 {1, 2, 3}。
 *
 * 【限制】
 * 时间限制：C/C++ 1秒，其他语言 2秒
 * 空间限制：C/C++ 256MB，其他语言 512MB
 */

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;
    while (T--) {
        int n;
        cin >> n;
        vector<int> a(n);
        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }

        // TODO: 在此处实现你的解法

    }
    return 0;
}
