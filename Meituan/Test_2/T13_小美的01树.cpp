/**
 * 第 13 题：小美的 01 树
 *
 * 【题目描述】
 * 小美有一棵节点编号为 1 ~ n 的树，每个节点只有 {0, 1} 这两种值之一。
 *
 * 设 u → v 为节点 u 到节点 v 的简单路径。
 * g(u → v) 为从 u 开始到 v 结束的简单路径上经过的所有点（包括 u, v）
 * 按照先后顺序组成的 01 字符串对应的十进制数对 (10^9 + 7) 取模的结果。
 *
 * 例如，简单路径 u → v 经过所有节点组成的字符串为 "01101"，其对应十进制就是 13，
 * 因此 g(u → v) = 13 mod (10^9 + 7) = 13。
 *
 * 小美会进行 m 次以下操作：
 *   - 操作 1：将简单路径 u → v 上所有节点的值反置。
 *   - 操作 2：询问 g(u → v) 的值。
 *
 * 你需要对小美的每一个操作 2 进行回答。
 *
 * 【反置】若当前字符为 0，反置后为 1；若当前字符为 1，反置后为 0。
 *
 * 【输入描述】
 * 第一行输入两个整数 n, m (1 <= n, m <= 2×10^5) 表示树的大小以及操作次数。
 * 第二行输入 n 个整数 a_i (a_i ∈ {0, 1}) 表示第 i 个节点初始的值。
 *
 * 接下来 n-1 行，每一行输入两个整数 u_i, v_i (1 <= u_i, v_i <= n)
 * 表示节点 u_i 与 v_i 之间有一条边。
 *
 * 接下来 m 行，每一行输入三个整数 x, u, v (x ∈ {1, 2}, 1 <= u, v <= n)，具体的：
 *   - x = 1：将简单路径 u → v 上所有节点的值反置。
 *   - x = 2：询问 g(u → v) 的值。
 *
 * 【输出描述】
 * 对于每个操作 2，在一行上输出一个整数，表示 g(u → v) 的值。
 *
 * 【示例】
 * 输入：
 * 5 5
 * 0 0 0 0 0
 * 1 2
 * 1 3
 * 2 4
 * 2 5
 * 2 1 4
 * 1 1 3
 * 2 4 1
 * 1 2 5
 * 2 5 1
 *
 * 输出：
 * 0
 * 1
 * 7
 *
 * 【说明】
 * 第一次询问时得到的字符串为 "000"，对应十进制为 0。
 * 第二次询问得到的字符串为 "001"，对应十进制为 1。
 * 第三次询问得到的字符串为 "111"，对应十进制为 7。
 *
 * 【限制】
 * 时间限制：C/C++ 3秒，其他语言 6秒
 * 空间限制：C/C++ 512MB，其他语言 1024MB
 */

#include <bits/stdc++.h>
using namespace std;

const int MOD = 1e9 + 7;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, m;
    cin >> n >> m;

    vector<int> a(n + 1);
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }

    vector<vector<int>> adj(n + 1);
    for (int i = 0; i < n - 1; i++) {
        int u, v;
        cin >> u >> v;
        adj[u].push_back(v);
        adj[v].push_back(u);
    }

    while (m--) {
        int x, u, v;
        cin >> x >> u >> v;

        if (x == 1) {
            // TODO: 实现路径反置操作
        } else {
            // TODO: 实现路径查询操作

        }
    }

    return 0;
}
