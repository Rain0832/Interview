# 美团暑期实习笔试 - 选择题

---

## 第 1 题（单选）

工具调用里经常出现"参数缺字段/类型不对"，更常见的处理是（）

- A. 把 temperature 调高，让模型更敢补全参数字段
- B. 收紧 schema 并做校验，失败则重试或回退
- C. 把 history 拼得更长，让模型看到更多上下文
- D. 减少工具数量，让模型每次只会调用一个工具

---

## 第 2 题（单选）

推理稳定性里"熔断/降级"更常见的含义是（）

- A. 输出慢时调高 temperature，让模型更快输出
- B. 高延迟或异常时切到小模型或模板答案，保证可用性
- C. 把 top_p 调到 1，让输出固定减少波动
- D. 把 prompt 变短，让模型永远不会超上下文窗口

---

## 第 3 题（单选）

LoRA 微调后，模型对少数方言理解下降，但普通话不受影响。更合适的补救是（）

- A. 把 tokenizer 词表扩很大，方言自然会被覆盖并能理解
- B. 把方言样本集中到最后几轮训练，强行让模型记住方言
- C. 混合采样维持方言占比，并小步继续微调防止被淹没
- D. 把 temperature 调高一点，让模型更敢输出方言相关内容

---

## 第 4 题（单选）

做 CLIP 式图文对齐预训练，"文本搜图"很差，但单模态看起来正常。更可疑的问题是（）

- A. 图像切 patch 的方式不对，应该改成整图 CNN 才能对齐
- B. 正负样本对构造错配或噪声高，导致对比学习目标失效
- C. 文本不该分词，按字符输入才能学到更稳定的对齐表示
- D. embedding 维度偏小，翻倍后跨模态检索一定会明显提升

---

## 第 5 题（单选）

你正在设计一个基于大语言模型的 AI 代码助手。在生成代码时，为了让模型输出既有创造性又不过于离谱，你需要调整一个关键参数来控制生成结果的随机性。这个参数通常被称为？

- A. 上下文窗口（Context Window）
- B. 温度（Temperature）
- C. 批处理大小（Batch Size）
- D. 学习率（Learning Rate）

---

## 第 6 题（单选）

在传输层中，TCP 协议提供了保证数据有序传输的机制。请问该机制的实现依赖于哪个特性？

- A. TCP 报文段中的序列号（Sequence Number）
- B. TCP 报文段中的时间戳（Timestamp option）
- C. TCP 报文段中的校验和（Checksum）
- D. IP 报文头中的源地址和目的地址

---

## 第 7 题（单选）

物流公司开发智能分拣系统时，某路径规划算法在计算 n 个配送点优化路线时，每增加一个配送点需处理四维空间数据。算法耗时模型为：

$$T(n) = T(n - 1) + 5n^4 \quad (n > 1), \quad T(1) = O(1)$$

该算法处理 n 个配送点的整体时间复杂度为（）

- A. $O(n^5)$
- B. $O(n^4)$
- C. $O(n^4 \log n)$
- D. $O(n^3)$

---

## 第 8 题（单选）

线程 T1 调用 `sem_wait(&S)` 后阻塞（初始值为 0），此时信号量 S 的值为？

- A. 0
- B. -1
- C. 1
- D. 取决于初始值

---

## 第 9 题（单选）

关于因果语言建模（CLM）的描述，正确的是（）

- A. GPT 系列模型采用从左到右的单向注意力（因果掩码）
- B. 损失函数仅计算序列最后一个 token 的预测
- C. CLM 训练时必须使用采样生成的 token 作为下一步输入
- D. CLM 原生只能用于分类任务，不能用于生成任务

---

## 第 10 题（单选）

若一棵二叉树的前序遍历序列为 `b, c, a, e, f, d`，后序遍历序列为 `a, f, d, e, c, b`，则根结点的孩子结点（）

- A. 只有 c
- B. 有 e、d
- C. 有 c、e
- D. 无法确定
