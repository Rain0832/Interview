/**
 * 第 12 题：超级斐波那契数列
 *
 * 【题目描述】
 * 定义超级斐波那契数列如下：给定整数 k，该序列的前 k 项均为 1；
 * 对于 n > k，第 n 项为前 k 项之和，即
 *
 *     S(n) = S(n-1) + S(n-2) + ... + S(n-k)
 *
 * 现给定整数 k 和查询次数 q，每次查询一个正整数 x，
 * 请输出该序列的第 x 项对 10^9 + 7 取模后的值。
 *
 * 【输入描述】
 * 第一行输入两个整数 k, q (1 <= k <= 10^6; 1 <= q <= 3×10^5)；
 * 此后 q 行，每行输入一个正整数 x (1 <= x <= 10^6)。
 *
 * 【输出描述】
 * 输出 q 行，每行输出一个整数，表示对应查询的答案对 10^9 + 7 取模后的值。
 *
 * 【示例】
 * 输入：
 * 2 5
 * 1
 * 2
 * 3
 * 4
 * 5
 *
 * 输出：
 * 1
 * 1
 * 2
 * 3
 * 5
 *
 * 【说明】
 * 当 k = 2 时，就是普通的斐波那契数列：
 *   S(1) = 1, S(2) = 1,
 *   S(3) = S(2) + S(1) = 2,
 *   S(4) = S(3) + S(2) = 3,
 *   S(5) = S(4) + S(3) = 5。
 *
 * 【限制】
 * 时间限制：C/C++ 1秒，其他语言 2秒
 * 空间限制：C/C++ 256MB，其他语言 512MB
 */

#include <bits/stdc++.h>
using namespace std;

const int MOD = 1e9 + 7;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int k, q;
    cin >> k >> q;

    // TODO: 在此处实现你的解法
    // 提示：可以预处理所有 S(x) 值，利用前缀和优化递推

    while (q--) {
        int x;
        cin >> x;

        // TODO: 输出 S(x) % MOD

    }

    return 0;
}
