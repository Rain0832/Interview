/**
 * 第 13 题：最大节点权值
 *
 * 【题目描述】
 * 给你一个由 n 个编号为 1 ~ n 的节点以及 m 条编号为 1 ~ m 的边组成的无向图，
 * 我们定义一个节点的权值为它的当前度（即已执行完之前所有操作后的状态）加上它的节点编号。
 *
 * 小美会进行 q 次如下操作：
 *   - 操作一：断开编号为 x 的边，保证每条边至多被删除一次，
 *             即在进行操作一时，该边当前一定存在于图中。
 *   - 操作二：向你询问编号为 x 的节点所在的连通块中所有节点中最大的权值，
 *             你需要将此权值输出。
 *
 * 【名词解释】
 * 度：与一个顶点相连接的边的条数称为该顶点的度。
 * 连通块（连通分量）：满足以下条件的原图子图：
 *   - 连通块内的任意两个顶点之间都存在路径相连，且路径上的点也在连通块内；
 *   - 是极大的，即不能再通过添加原图中的其他顶点而依旧保持连通性；
 *   - 单独的点也构成一个连通块。
 *
 * 【输入描述】
 * 第一行输入三个正整数 n, m, q
 *   (1 <= n, q <= 2×10^5; 0 <= m <= min(n*(n-1)/2, 2×10^5))
 *   表示节点个数、边个数、操作次数。
 *
 * 此后 m 行，第 i 行输入两个整数 u_i 和 v_i (1 <= u_i, v_i <= n; u_i != v_i)
 *   表示图上第 i 条边连接节点 u_i 和 v_i。
 *
 * 此后 q 行，第 i 行先输入一个整数 o_i (1 <= o_i <= 2)，表示操作编号：
 *   - 若 o_i = 1，输入一个整数 x_i (1 <= x_i <= m)，表示断掉的边的编号；
 *   - 若 o_i = 2，输入一个整数 x_i (1 <= x_i <= n)，表示询问的节点编号。
 *
 * 保证图没有重边和自环，操作一合法。
 *
 * 【输出描述】
 * 输出若干行，每一行对操作二进行回答。
 *
 * 【示例】
 * 输入：
 * 5 5 5
 * 1 2
 * 1 5
 * 3 5
 * 2 4
 * 1 3
 * 2 4
 * 1 1
 * 2 2
 * 1 2
 * 2 1
 *
 * 输出：
 * 7
 * 5
 * 6
 *
 * 【提示】
 * 考虑离线处理 + 并查集：先记录所有要删的边，从最终状态（所有要删的边都已删）
 * 开始反向加边，用并查集维护连通块和最大权值。
 * 注意：删边时节点度数会变化，权值也会变化。
 *
 * 【限制】
 * 时间限制：C/C++ 1秒，其他语言 2秒
 * 空间限制：C/C++ 256MB，其他语言 512MB
 */

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, m, q;
    cin >> n >> m >> q;

    vector<pair<int, int>> edges(m + 1);
    for (int i = 1; i <= m; i++) {
        cin >> edges[i].first >> edges[i].second;
    }

    vector<pair<int, int>> ops(q);  // {操作类型, x}
    for (int i = 0; i < q; i++) {
        cin >> ops[i].first >> ops[i].second;
    }

    // TODO: 在此处实现你的解法
    // 提示：离线 + 反向操作（删边变加边）+ 并查集

    return 0;
}
